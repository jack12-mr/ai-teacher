# 管理后台脚本使用指南

## 📋 概述

本目录包含管理后台相关的初始化和管理脚本。

## 🚀 快速开始

### 1. 初始化数据库

#### Supabase (国际版)

1. 登录 Supabase 控制台
2. 打开 SQL Editor
3. 复制并执行 `supabase/migrations/admin_tables.sql` 中的 SQL 语句

#### CloudBase (国内版)

```bash
npm run init-admin-db
```

### 2. 创建管理员账户

```bash
npm run create-admin
```

脚本会提示你输入：
- 用户名（至少 3 位，只能包含字母、数字、下划线和连字符）
- 密码（至少 8 位，必须包含字母和数字）
- 角色（admin 或 super_admin）

### 3. 登录管理后台

访问：`http://localhost:3000/admin/login`

## 📝 详细说明

### 数据库初始化脚本

#### Supabase (`supabase/migrations/admin_tables.sql`)

创建以下表：
- `admin_users` - 管理员用户表
- `system_logs` - 系统操作日志表
- `system_config` - 系统配置表

#### CloudBase (`scripts/init-admin-cloudbase.js`)

创建以下集合：
- `admin_users` - 管理员用户集合
- `system_logs` - 系统操作日志集合
- `system_config` - 系统配置集合

### 管理员创建脚本 (`scripts/create-admin.ts`)

支持以下选项：

```bash
# 交互式创建（推荐）
npm run create-admin

# 指定用户名和密码
npm run create-admin -- -u admin -p password123

# 指定角色
npm run create-admin -- -r super_admin

# 跳过确认
npm run create-admin -- --force

# 指定数据库类型
npm run create-admin -- --cloudbase  # 仅 CloudBase
npm run create-admin -- --supabase   # 仅 Supabase
```

## 🔐 安全建议

1. **首次部署后立即修改默认管理员密码**
2. **使用强密码**（至少 8 位，包含字母和数字）
3. **定期更换密码**
4. **限制管理员账户数量**
5. **启用操作日志审计**

## 🛠️ 故障排除

### 问题：数据库连接失败

**解决方案**：
- 检查 `.env.local` 中的环境变量是否正确配置
- 确认数据库服务是否正常运行
- 检查网络连接

### 问题：用户名已存在

**解决方案**：
- 使用不同的用户名
- 或直接在数据库中修改现有用户

### 问题：密码不符合要求

**解决方案**：
- 确保密码至少 8 位
- 确保密码包含字母和数字

## 📚 相关文档

- [管理后台开发文档](../docs/admin.md)
- [数据库架构](../docs/database-schema.md)
- [API 文档](../docs/api.md)
